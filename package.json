{
  "name": "hackaton-stack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "docker compose up --build",
    "dev:web": "cd apps/web && npm run dev",
    "dev:mcp": "cd apps/mcp-server && npm run dev",
    "dev:browser": "cd apps/browser-worker && npm run start"
  }
}
